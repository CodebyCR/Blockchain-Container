import XCTest
@testable import Blockchain_Container

final class Blockchain_ContainerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Blockchain_Container().text, "Hello, World!")
    }
}
